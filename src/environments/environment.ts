// src/environments/environment.ts
export const environment = {
  production: true,
  apiBaseUrl: '', // Vacío porque usaremos rutas relativas
  caregiverEndpoint: '/api/caregivers',
  userEndpoint: '/api/users',
  subscriptionEndpoint: '/api/subscriptions',
  supportEndpoint: '/api/support',
  historyEndpoint: '/api/history'
};
